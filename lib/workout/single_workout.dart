import 'package:user2/user/user.dart';

class SingleWorkout {
  late String pictureUrl;
  late String nameOfWorkout;
  late int numberOfSets;
  late int timesOnSingleSet;

  SingleWorkout(AppUser appUser, String nameOfWorkout) {
    // TODO: create an appropriate algorithm, which will access to DB and build appropriate params for user.
  }
}